interface:
  display_name: "Triage"
  short_description: "Move issues through triage roles"
  default_prompt: "Use $triage to classify and verify this issue before preparing its next state."
policy:
  allow_implicit_invocation: false
